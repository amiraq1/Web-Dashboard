المنصة تتكوّن من 6 أجزاء رئيسية:

واجهة المستخدم (Web App / Dashboard)

طبقة فهم الأوامر (NLU / LLM Layer)

منسّق المهام والمشاريع (Task / Project Orchestrator)

وكلاء تنفيذ (Agents / Tools) يتعاملون مع البيانات والخدمات

طبقة البيانات (DB + Vector Store + Files)

نظام الهويّة والصلاحيات (Auth & Permissions)

تخيّلها كالتالي:

المستخدم → واجهة ويب → طبقة الفهم اللغوي → المنسّق → الوكلاء → البيانات / الخدمات → نتيجة ترجع للواجهة

2. واجهة المستخدم (Frontend)
أهداف الواجهة:

مكان واحد لإدارة:

الأوامر الفورية (Chat / Command Bar)

المشاريع (Projects / Workflows)

الملفات والبيانات المتصلة

النتائج والتقارير

عناصر UI أساسية:

شريط أوامر ذكي:

يقبل أوامر مثل:

"حلّل ملف المبيعات وطلع لي أهم 5 منتجات"

"أنشئ تقرير أسبوعي لمشروع X واعتمد على ملفات Notion وGoogle Sheets"

لوحة مشاريع (Projects):

كل مشروع له: حالة، مهام، نسبة إنجاز، الوكلاء اللي يشتغلون عليه، تاريخ آخر تحديث.

مستعرض ملفات وبيانات (Data Browser):

Google Drive / Notion / DB / رفع ملفات يدوي.

تايم لاين للتنفيذ (Execution Timeline):

تظهر تسلسل المهام، الأخطاء، المحاولات، retries.

تقنيات مقترحة:

React أو Next.js (مع SSR لو حاب SEO أو أداء أعلى).

Tailwind CSS أو shadcn/ui لتسريع الـ UI.

Zustand / Jotai / Redux Toolkit لإدارة الحالة الجانبية (state).

3. طبقة فهم الأوامر (Natural Language Understanding Layer)

هذه هي “دماغ فهم الكلام”.

ماذا تفعل؟

تستقبل نص المستخدم.

تستخرج:

النية (Intent): مثل: تحليل، إنشاء تقرير، تلخيص، ربط بيانات، تشغيل سكربت…

الكيانات (Entities): ملفات معيّنة، مشروع، نطاق زمني، نوع مخرج (PDF, جدول, Markdown)، إلخ.

مستوى العمل: أمر واحد؟ أم مشروع/Workflow مستمر؟

كيف تُبنى؟

نموذج لغوي (LLM) + طبقة “أدابتور” منطقية فوقه.

يمكنك:

إما استخدام API جاهزة (OpenAI, Anthropic, إلخ).

أو تشغيل نموذج مفتوح المصدر ذاتيًا (مثل Llama / Qwen) عبر خادم مخصص.

ناتج هذه الطبقة:

غالبًا JSON من النوع:

{
  "intent": "analyze_sales",
  "project": "Q1 Report",
  "inputs": {
    "files": ["gsheet:123", "drive:abc"],
    "time_range": "2025-Q1"
  },
  "output_format": "markdown_report",
  "priority": "normal"
}


هذا JSON يروح لمنسّق المهام.

4. منسّق المهام والمشاريع (Task / Project Orchestrator)

هذا هو القلب التشغيلي للمنصة، يقابل اللي تحدثنا عنه سابقاً كـ Task Orchestrator / Agent Coordinator.

وظائفه:

تحويل نية واحدة إلى:

Tasks (مهام صغيرة).

أو Workflow (تسلسل مهام + شروط + تكرار).

توزيع المهام على الوكلاء (Agents).

متابعة الحالة: Pending / Running / Failed / Completed.

التعامل مع:

إعادة المحاولة (Retries).

مهام متوازية (Parallel).

مهام تعتمد على نتائج مهام سابقة.

نموذج بسيط للمهام:
{
  "id": "task-001",
  "type": "query_data",
  "agent": "DataAgent",
  "status": "pending",
  "inputs": { "source": "gsheet:123", "query": "sales by product" },
  "next": ["task-002"]
}

5. الوكلاء (Agents / Tools Layer)

كل وكيل هو “عامل ذكي” متخصص في نوع معين من العمل.

أمثلة لوكلاء:

DataAgent

يتعامل مع PostgreSQL, MySQL, Google Sheets, CSV.

يحوّل سؤال اللغة الطبيعية إلى استعلام SQL أو معالجة Pandas.

FileAgent

يقرأ ملفات PDF, DOCX, TXT.

يبني embeddings ويخزنها في Vector DB.

يبحث داخل الملفات حسب السياق.

WebAgent

يتعامل مع APIs خارجية، Scraping إن لزم (مع احترام القوانين).

ReportAgent

ينسّق النتائج في تقارير Markdown / HTML / PDF.

كيف يتواصل المنسّق مع الوكلاء؟

عبر رسائل أو أوامر (Command objects).

يمكنك تطبيق:

Pattern مثل: Command + Handler لكل نوع مهمة.

التنفيذ يتم غالبًا في:

Workers / Job Queue (مثل BullMQ, Redis, أو Sidekiq-like).

6. طبقة البيانات (Data Layer)
تحتاج 3 أنواع من التخزين:

قاعدة بيانات علائقية (PostgreSQL مثلاً)

المستخدمين، المشاريع، المهام، حالة التنفيذ، إعدادات الحساب، الـ OAuth tokens.

Vector Database (مثل Qdrant, Pinecone, Weaviate, Chroma)

لتخزين تمثيلات نصية (Embeddings) للملفات، الوثائق، الملاحظات، mail، إلخ.

تُستخدم لبحث سياقي ذكي:
“هات كل ما كتبته عن مشروع X” بدون ما تحتاج تتذكر أسماء الملفات.

تخزين ملفات (Object Storage)

S3 / GCS / MinIO أو حتى نظام ملفات محلي للمراحل الأولى.

7. الهوية والصلاحيات (Auth & Permissions)

منصة مثل Manus AI تتعامل مع بيانات حساسة → الأمان مهم جداً.

تسجيل الدخول:

Email + Password

أو OAuth (Google, GitHub, Microsoft…)

ربط الحسابات:

Google Drive, Notion, Slack, Jira, GitHub, Databases…

يستخدم OAuth 2.0 / OpenID Connect.

إدارة الصلاحيات:

لكل مستخدم: ما هي المصادر المسموحة له؟

لكل مشروع: من يقدر يشوفه أو يعدله؟

8. ستاك تقني مقترح (Stack Suggestion)
Frontend

Next.js (React)

Tailwind CSS + shadcn/ui

Zustand / Redux Toolkit لإدارة الحالة

Backend

Node.js (NestJS / Express / Fastify) أو
Python (FastAPI / Django) حسب تفضيلك.

Job Queue: BullMQ + Redis أو Celery + Redis.

AI Layer

LLM API (OpenAI كبداية)

Vector DB: Qdrant / Chroma

Embeddings: عبر API أو نموذج محلي.

Data

PostgreSQL كقاعدة بيانات أساسية

S3-compatible storage للملفات

9. خارطة طريق عملية (Roadmap مختصرة)
المرحلة 1 – MVP (نواة المنصة)

واجهة Chat / Command بسيطة.

دعم رفع ملفات نصية / PDF + تخزينها.

LLM يجاوب على الأسئلة اعتماداً على ملف واحد (RAG بسيط).

حفظ المشاريع البسيطة (سؤال + نتيجة).

المرحلة 2 – مشاريع + مهام

إضافة Project Orchestrator.

دعم مهام متعددة في مشروع واحد (مثلاً: قراءة ملف → تحليل → كتابة تقرير).

واجهة Projects + Tasks Timeline.

المرحلة 3 – Data Connectors

ربط Google Drive / Notion / DB.

Agent للبيانات: تحويل سؤال → SQL / استعلام Sheets.

Vector DB للبحث السياقي في كل المحتوى.

المرحلة 4 – وكيل متعدد الخطوات (Multi-step Agent)

دعم أوامر معقدة يقوم فيها النظام:

بالتخطيط (plan)

ثم التنفيذ خطوة خطوة

وتعديل الخطة عند ظهور أخطاء.

إذا تحب، في الرسالة الجاية أقدر أعمل لك:

تصميم API حقيقي للـ Orchestrator و Agents (بـ TypeScript أو Python)،

أو هيكل مشروع Next.js + NestJS جاهز كبداية للمنصة 👨‍💻✨

أنت قلت:
اسم الوكيل نبض 